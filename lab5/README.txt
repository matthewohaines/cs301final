André Shannon

Computer Organization (301)
Lab5 (due Oct 22, 2020)

I was partners with Jordan Secret, but he dropped the course, so I did the lab by myself.

Use make to compile and get an executable named ASM. To run, command should look like:
./ASM myAssemblyCode.asm
