#include "PCCounter.h"
#include<iostream>
using namespace std;

RegisterFile::RegisterFile(string filename)
{
	// get first two numbers, set as i, then for array[i], store the hex values
}

RegisterFile::~RegisterFile(){}